{
  "name": "guatapay",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "app",
    "server"
  ],
  "scripts": {
    "predev": "node scripts/prepare-app-env.js",
    "dev": "concurrently -n server,app -c blue,green \"npm run dev --workspace=server\" \"npm run start --workspace=app\"",
    "prepare": "husky"
  },
  "devDependencies": {
    "concurrently": "^9.0.1",
    "husky": "^9.1.7",
    "lint-staged": "^17.5.1",
    "prettier": "^3.9.6"
  }
}
